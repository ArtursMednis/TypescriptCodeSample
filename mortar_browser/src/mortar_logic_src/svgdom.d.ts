// Create a file named 'svgdom.d.ts' (or any other name you prefer) in your project

declare module 'svgdom' {
  export function createSVGWindow(): Window;
  // Add other exported functions or types from the module as needed
}
